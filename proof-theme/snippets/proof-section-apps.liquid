{%- if section.blocks.size > 0 -%}
  <div class="proof-section-apps">
    {%- for block in section.blocks -%}
      <div class="proof-section-apps__block" {{ block.shopify_attributes }}>
        {% render block %}
      </div>
    {%- endfor -%}
  </div>
{%- endif -%}
