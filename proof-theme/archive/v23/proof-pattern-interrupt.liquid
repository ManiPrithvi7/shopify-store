{% render 'proof-checkout-url', section: section %}
<!-- ══ PATTERN INTERRUPT ══ -->
<div style="padding:56px 60px;border-top:1px solid rgba(255,255,255,.04);border-bottom:1px solid rgba(255,255,255,.04);background:var(--surface-base);">
  <div style="max-width:900px;margin:0 auto;display:flex;align-items:center;gap:48px;">
    <div style="flex-shrink:0;width:2px;height:80px;background:linear-gradient(to bottom,transparent,rgba(201,168,76,.4),transparent);"></div>
    <div>
      <p style="font-family:var(--font-display);font-size:clamp(22px,2.8vw,34px);font-weight:700;color:var(--neutral-100);line-height:1.3;letter-spacing:-.02em;">Every customer already sees your counter.<br><span style="color:rgba(201,168,76,.8);">Most don't know you have 247 five-star reviews.</span></p>
      <p style="font-size:14px;color:rgba(184,184,173,.5);margin-top:14px;font-weight:300;line-height:1.65;">PROOF puts your reputation where your customers already look.</p>
    </div>
  </div>
</div>
{% render 'proof-section-apps' %}

{% schema %}
{
  "name": "PROOF Pattern interrupt",
  "tag": "section",
  "class": "proof-section",
  "settings": [
    { "type": "product", "id": "product", "label": "Checkout product" },
    { "type": "url", "id": "demo_video_url", "label": "90-sec demo link" }
  ],
  "blocks": [{ "type": "@app" }],
  "presets": [{ "name": "PROOF Pattern interrupt" }]
}
{% endschema %}
